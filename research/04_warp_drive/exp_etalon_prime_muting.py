"""
The Fabry-Perot etalon trick: CORRECT way to mute even modes
==============================================================

Key insight: a Fabry-Perot etalon of optical thickness a/2
is TRANSPARENT to even cavity modes and REFLECTIVE to odd ones.

Replace one mirror with this etalon:
  Even modes (2,4,6,...) → see transparent wall → escape → MUTED
  Odd modes (1,3,5,...) → see mirror → stay → SURVIVE

This gives ζ_{¬2} correctly. And it's commercial optical technology.

Wright Brothers, 2026
"""

import numpy as np
pi = np.pi
hbar = 1.054571817e-34
c = 2.99792458e8

print("=" * 70)
print("  ファブリ-ペロー・エタロンによる素数ミュート")
print("=" * 70)

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ アイデア
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ファブリ-ペロー・エタロン:
    2枚の半透明ミラーの間に薄い空間（厚さ d）。
    特定の周波数で透過が最大（共鳴透過）。

  透過率:
    T(f) = 1 / (1 + F sin²(πf/FSR))
    FSR = c/(2d) = 自由スペクトル域

  ── エタロンの厚さ d = a/2 にすると ──

  FSR = c/(2 × a/2) = c/a = 2f₁

  （ここで f₁ = c/(2a) はキャビティの基本モード周波数）

  各モード周波数での透過率:
""")

# Cavity length a, etalon thickness d = a/2
# Finesse F = 30 (typical for a decent etalon)
F_etalon = 30

print(f"  エタロンのフィネス F = {F_etalon}")
print()
print(f"  {'モード n':>10s} {'f/f₁':>8s} {'sin²(πf/FSR)':>16s} {'透過率 T':>10s} {'状態':>10s}")
print(f"  {'-'*58}")

for n in range(1, 11):
    # f = n × f₁, FSR = 2f₁
    # sin²(πf/FSR) = sin²(πnf₁/(2f₁)) = sin²(nπ/2)
    sin2 = np.sin(n * pi / 2)**2
    T = 1 / (1 + F_etalon * sin2)

    if T > 0.9:
        status = "★ 透過（ミュート）"
    elif T < 0.1:
        status = "反射（生存）"
    else:
        status = "部分的"

    parity = "偶数" if n % 2 == 0 else "奇数"
    print(f"  n={n:>2d} ({parity}) {n:>8d}f₁ {sin2:>16.4f} {T:>10.4f} {status:>10s}")

print(f"""
  ★★★ 完璧な分離！

  偶数モード: 透過率 ≈ 1（エタロンを素通り → キャビティから脱出 → ミュート）
  奇数モード: 透過率 ≈ {1/(1+F_etalon):.4f}（反射 → キャビティ内に閉じ込め → 生存）

  残るモード: 1, 3, 5, 7, 9, ...
  和: ζ_{{¬2}}(-1) = 1 + 3 + 5 + 7 + ... = +1/12 → 符号反転！
""")

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ なぜこれが機能するか
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  カシミール・キャビティ:
    左壁: 完全反射ミラー（全モードを反射）
    右壁: ファブリ-ペロー・エタロン（厚さ a/2）

  偶数モード（n=2,4,6,...）:
    周波数 f = 2mf₁ → エタロンの共鳴条件に一致
    → エタロンを透過 → キャビティの「外」に出る
    → 閉じ込められない → ゼロ点エネルギーに寄与しない
    → ミュート！

  奇数モード（n=1,3,5,...）:
    周波数 f = (2m+1)f₁ → エタロンの反射条件に一致
    → エタロンで反射 → キャビティ内に閉じ込められる
    → ゼロ点エネルギーに寄与する
    → 生存！

  ★ 空間的な trick（L/2 に何かを置く）ではなく、
    境界条件の周波数選択性を使っている。
    偶数/奇数の分離は完璧（sin²(nπ/2) で正確に 0 か 1）。
""")

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ 実験設計 v5: カシミール + エタロン
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Config A（標準カシミール）:
    2枚の完全反射ミラー、間隔 a
    → 標準カシミール引力 F ∝ ζ(-3) > 0...
    いや、カシミール力は負（引力）。

  Config B（素数2ミュート）:
    左壁: 完全反射ミラー
    右壁: エタロン（光学厚さ a/2）
    → 偶数モードが脱出 → 奇数モードのみ
    → 力 ∝ ζ_{¬2}(-3) = -7/120 < 0...

  ★ 待て。符号の確認が必要。
""")

# Careful sign analysis
# Standard Casimir: E(a) = -(πℏc)/(24a) × (per unit area in 1D... actually)
# In 3D: F/A = -π²ℏc/(240 a⁴) (negative = attractive)
# The "240" comes from the full ζ(-3) calculation.

# With only odd modes:
# E_odd = (ℏ/2) Σ_{n_z odd} Σ_{k⊥} √(k⊥² + (n_z π/a)²)
# After regularization: this involves Σ_{n odd} n^{-s} at appropriate s.

# For 3D: the sum over n_z gives a factor ζ_R(-3) where R is the relevant sum.
# All n: ζ(-3) = 1/120
# Odd n only: ζ_{¬2}(-3) = (1-2³)/120 = -7/120

# Standard Casimir pressure:
# P = -π²ℏc/(240 a⁴) = -(π²ℏc)/(2a⁴) × ζ(-3)
# (the 2 comes from two polarizations)

# With odd modes only:
# P_odd = -(π²ℏc)/(2a⁴) × ζ_{¬2}(-3) = -(π²ℏc)/(2a⁴) × (-7/120)
#       = +(7π²ℏc)/(240 a⁴)

a = 1e-6  # 1 μm
P_standard = -pi**2 * hbar * c / (240 * a**4)
P_odd = -pi**2 * hbar * c / (2 * a**4) * (-7/120)

print(f"  ── 力の計算 ──")
print()
print(f"  標準カシミール（全モード）:")
print(f"    P = -π²ℏc/(240a⁴) = {P_standard:.4f} Pa（引力）")
print()
print(f"  エタロン設置（奇数モードのみ）:")
print(f"    P = +(7/240)×π²ℏc/a⁴ = {P_odd:.4f} Pa")
print()

if P_odd > 0:
    print(f"  ★★★ 斥力！ 符号が反転した！")
    print(f"    引力 ({P_standard:.4f} Pa) → 斥力 ({P_odd:.4f} Pa)")
    print(f"    比: P_odd/P_standard = {P_odd/P_standard:.1f}")
else:
    print(f"  まだ引力。符号反転なし。")

print()

# Detectable?
precision = 0.001  # 0.1% precision of modern Casimir experiments
signal = abs(P_odd - P_standard) / abs(P_standard) * 100

print(f"  検出可能性:")
print(f"    力の変化: |ΔP/P| = {signal:.0f}%")
print(f"    既存精度: 0.1%")
print(f"    S/N: {signal/0.1:.0f}")
print()

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ 具体的な装置
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  2つの配置を AFM で力を測り比べるだけ。
""")

for a_nm in [200, 500, 1000, 2000]:
    a_m = a_nm * 1e-9
    d_etalon = a_m / 2
    f1 = c / (2*a_m)
    P = abs(pi**2 * hbar * c / (240 * a_m**4))

    print(f"  a = {a_nm} nm:")
    print(f"    エタロン厚さ d = {d_etalon*1e9:.0f} nm")
    print(f"    基本モード f₁ = {f1/1e12:.0f} THz (λ₁ = {c/f1*1e9:.0f} nm)")
    print(f"    カシミール圧力 |P| = {P:.2f} Pa")
    print()

print(f"""
  ── エタロンの実現方法 ──

  光学厚さ a/2 のエタロン = 2枚の半透明膜 + 間のスペーサー。

  a = 1000 nm の場合: エタロン厚さ d = 500 nm。
  これは光学薄膜フィルタの標準的な厚さ。

  具体的に:
  (1) 誘電体多層膜（SiO₂/TiO₂ etc.）で半透明ミラーを形成
  (2) その間に厚さ d の誘電体スペーサーを挟む
  (3) 全体を1つの「右壁」として配置

  → 光学薄膜のコーティング会社（例: Thorlabs, Edmund Optics）が
    標準的に製造できる製品。

  ただし: 通常のエタロンは基板（mm厚のガラス）の上に成膜。
  カシミール実験では基板の厚さが問題になる
  （基板を通したカシミール力が弱くなる）。

  解決策:
  (a) 自立膜（基板なし）のエタロンを作る → 技術的に困難
  (b) 極薄基板（Si₃N₄ メンブレン, ~50 nm）上に成膜
      → Si₃N₄ メンブレンは MEMS/NEMS で標準的な材料
  (c) 球-平面配置で近接場測定
""")

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ circuit QED 版（Option 2）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  マイクロ波帯（GHz）なら周波数選択は格段に容易。

  3Dキャビティ（35mm角アルミ）:
    f₁ ≈ 6 GHz, f₂ ≈ 12 GHz, f₃ ≈ 18 GHz, f₄ ≈ 24 GHz, ...

  偶数モードをミュートするには:
    トランスモン 1: f₂ = 12 GHz に同調 → モード2をミュート
    トランスモン 2: f₄ = 24 GHz に同調 → モード4をミュート
    トランスモン 3: f₆ = 36 GHz に同調 → モード6をミュート
    （以下、キャビティのカットオフまで）

  各トランスモンはパーセル効果で対応モードを減衰させる。

  必要なトランスモン数:
    カットオフ ~50 GHz の場合: 偶数モードは 12, 24, 36, 48 GHz → 4個
    実現可能性: Google/IBM は 50-100+ 量子ビットを使う → 4個は容易

  問題:
  - 各トランスモンが偶数モードだけに結合し、
    奇数モードに結合しないことを保証する必要がある
  - 空間配置で分離するのは（偶奇問題で）困難
  - 周波数チューニングで選択するのが正しい方法
  - 各トランスモンの E_J を磁束で調整して正確に同調させる

  ★ 技術的には可能。しかしエタロン版より複雑で高コスト。
""")

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ v5 エタロン vs v3 circuit QED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
""")

print(f"  {'':>20s} {'v5 エタロン':>20s} {'v3 circuit QED':>20s}")
print(f"  {'-'*62}")
print(f"  {'動作温度':>20s} {'室温':>20s} {'10-300 mK':>20s}")
print(f"  {'偶奇分離':>20s} {'完璧（sin²=0,1）':>20s} {'良好（周波数同調）':>20s}")
print(f"  {'必要な装置':>20s} {'AFM + 薄膜エタロン':>20s} {'冷凍機+トランスモン':>20s}")
print(f"  {'コスト':>20s} {'10-30万?':>20s} {'5-40万+共同研究':>20s}")
print(f"  {'精度':>20s} {'~0.1%（AFM）':>20s} {'S/N > 10⁶':>20s}")
print(f"  {'シグナル/ノイズ':>20s} {f'{signal/0.1:.0f}':>20s} {'> 10⁶':>20s}")
print(f"  {'偶数モード数':>20s} {'全て（光帯域）':>20s} {'3-4個（GHz帯域）':>20s}")
print(f"  {'主な困難':>20s} {'自立エタロン製作':>20s} {'複数トランスモン製作':>20s}")
print()

print(f"""
  ★ v5（エタロン）が圧倒的に有利:
    - 室温で動作
    - 偶奇の分離が原理的に完璧
    - 光学薄膜は確立された技術
    - AFM は多くの大学にある

  v5 の唯一の困難:
    エタロンをカシミール用の極小スケール（~500 nm厚）で作り、
    それを片方の「壁」として使えるように配置すること。
    → MEMS/NEMS の薄膜技術で原理的に可能。
""")

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ 結論
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  偶数モードをミュートする方法が見つかった:
  ファブリ-ペロー・エタロン（厚さ a/2）を壁に使う。

  偶数モード → エタロンを透過 → 脱出 → ミュート
  奇数モード → エタロンで反射 → 閉じ込め → 生存

  分離は sin²(nπ/2) で正確に 0 か 1。
  偶奇の混同なし。空間的な trick ではなく周波数選択。

  予測:
  標準カシミール（全モード）: 引力 (P < 0)
  エタロン版（奇数のみ）: 斥力 (P > 0), 大きさ 7倍

  → 符号反転は AFM の精度で余裕で検出可能。
  → 室温、既存技術、低コスト。

  これが素数2ミュートの正しい実験設計 v5。
""")

print("=" * 70)
print("  END")
print("=" * 70)
