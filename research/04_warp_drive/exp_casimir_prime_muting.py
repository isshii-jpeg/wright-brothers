"""
Casimir effect AS prime muting: fundamental redesign
=====================================================

Key insight: inserting a wall at L/2 in a cavity kills all even modes.
This IS prime-2 muting: ζ(-1) → ζ_{¬2}(-1) = +1/12.

The Casimir effect has been measured. Does the data already contain
evidence of the sign flip?

Wright Brothers, 2026
"""

import numpy as np
pi = np.pi
hbar = 1.054571817e-34
c_light = 2.99792458e8

print("=" * 70)
print("  カシミール効果 = 素数ミュート？ 根本的再考")
print("=" * 70)

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ 壁を入れる = 素数ミュート の厳密な対応
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  1次元キャビティ（長さ L）のモード:
    f_n = n × c/(2L),  n = 1, 2, 3, 4, 5, ...

  真空エネルギー（ζ正則化）:
    E = (ℏ/2) Σ_n ω_n = (πℏc)/(2L) × Σ_n n
    = (πℏc)/(2L) × ζ(-1) = -(πℏc)/(24L)

  ── L/2 に壁を挿入 ──

  偶数モード（n=2,4,6,...）は L/2 で腹を持つ → 壁で殺される。
  奇数モード（n=1,3,5,...）は L/2 で節を持つ → 生き残る。

  壁挿入後の真空エネルギー:
    E' = (πℏc)/(2L) × Σ_{n odd} n
       = (πℏc)/(2L) × ζ_{¬2}(-1)
       = (πℏc)/(2L) × (+1/12)
       = +(πℏc)/(24L)

  ★ 符号が反転した！  E < 0 → E' > 0

  しかし待て。壁を入れると「2つの小さなキャビティ」ができる。
  各小キャビティの長さは L/2、そのモードは:
    f_m = m × c/(2×L/2) = m × c/L,  m = 1, 2, 3, ...

  各小キャビティの真空エネルギー:
    E_half = (πℏc)/(2×L/2) × ζ(-1) = -(πℏc)/(12×L) × (1/12)...

  いや、正確に計算しよう。
""")

# Careful calculation
# Original cavity: length L
# Modes: ω_n = nπc/L, n=1,2,3,...
# E_original = (ℏ/2) Σ_n nπc/L = (πℏc)/(2L) × ζ(-1)

# With wall at L/2: TWO cavities, each of length L/2
# Each has modes: ω_m = mπc/(L/2) = 2mπc/L, m=1,2,3,...
# E_each = (ℏ/2) Σ_m 2mπc/L = (πℏc/L) × ζ(-1)
# E_two = 2 × (πℏc/L) × ζ(-1) = 2πℏc/L × (-1/12) = -πℏc/(6L)

# The DIFFERENCE (Casimir energy):
# E_Casimir = E_two - E_original
#           = -πℏc/(6L) - (-πℏc/(24L))
#           = -πℏc/(6L) + πℏc/(24L)
#           = πℏc/L × (-1/6 + 1/24)
#           = πℏc/L × (-4/24 + 1/24)
#           = πℏc/L × (-3/24)
#           = -πℏc/(8L)

E_coeff_original = -1/24  # coefficient of πℏc/L in E_original (= ζ(-1)/2)
E_coeff_two = 2 * (-1/12)  # two half-cavities: 2 × ζ(-1) × (πℏc/L)
# Wait, I need to be more careful with the factors.

# Let me use a consistent normalization.
# Cavity of length a: E(a) = -(πℏc)/(24a) [1D Casimir energy per unit... actually just energy]
# This is the standard 1D result.

# Original: one cavity of length L.
# E_orig = -πℏc/(24L)

# With wall at L/2: two cavities of length L/2 each.
# E_with_wall = 2 × (-πℏc/(24×L/2)) = 2 × (-πℏc/(12L)) = -πℏc/(6L)

# Casimir energy of the wall:
# E_Casimir = E_with_wall - E_orig = -πℏc/(6L) - (-πℏc/(24L))
#           = -πℏc/(6L) + πℏc/(24L) = πℏc/L (-4+1)/24 = -3πℏc/(24L) = -πℏc/(8L)

E_orig = -1/24  # in units of πℏc/L
E_with_wall = -1/6
E_casimir_wall = E_with_wall - E_orig

print(f"  ── 正確な計算 ──")
print()
print(f"  元��キャビティ（長さ L）:")
print(f"    E_orig = -(πℏc)/(24L) [= ζ(-1)/2 × πℏc/L]")
print()
print(f"  壁挿入後（2つの L/2 キャビティ）:")
print(f"    E_wall = 2×(-(πℏc)/(24×L/2)) = -(πℏc)/(6L)")
print()
print(f"  カシミールエネルギー（壁の自己エネルギー）:")
print(f"    E_Cas = E_wall - E_orig = -{1/6:.4f} - (-{1/24:.4f}) = {E_casimir_wall:.4f}")
print(f"    = -(πℏc)/(8L)")
print()
print(f"  ★ カシミールエネルギーは負（= 壁は引かれる ← 既知の結果）")
print()

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ 「奇数モードの和」の解釈
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ζ_{¬2}(-1) = 1 + 3 + 5 + 7 + ... = +1/12

  しかしこれは「壁のないキャビティで奇数モードだけ残す」の
  真空エネルギーであって、
  「壁を入れたキャビティ」の真空エネルギーとは異なる！

  壁を入れると:
  (a) 偶数モードが消える → ζ_{¬2} 的
  (b) 小キャビティの新しいモードが生まれる → 追加の寄与

  つまり「壁を入れる」≠「偶数モードだけ消す」。
  壁は消すだけでなく、新しい境界条件を作り、
  新しいモード（小キャビティのモード）を生む。

  ★ これが核心的な違い:
    ζ_{¬2} = 「偶数を引き算」（純粋な引き算）
    壁挿入 = 「偶数を消し、新しいモードを追加」（引いて足す）
""")

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ 「純粋な素数ミュート」を物理的に実現する方法
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  壁はダメ（新しいモードが生まれるから）。
  SQUIDは不十分（1つのモードしか消せない）。

  必要なのは: 「偶数モードを全て吸収するが、
  新しいモードを作らない」デバイス。

  ── 候補 1: 周波数選択的吸収体 ──

  偶数倍の周波数だけを吸収する素材/回路。
  f₂, f₄, f₆, ... を全て吸収。奇数には透明。

  実現方法: 周期構造（フォトニック結晶的）
  周期 L/2 の構造はキャビティの偶数モードと共鳴。
  → しかしこれも実質的に「壁」に近い。

  ── 候補 2: 複数 SQUID ──

  N 個の SQUID を異なる偶数モードにチューニング。
  SQUID₁ → f₂, SQUID₂ → f₄, SQUID₃ → f₆, ...

  必要な数: キャビティのモード数に依存。
  f_cutoff = 100 GHz なら偶数モードは ~8 個。
  8個の SQUID を同時にチューニングする。

  技術的に可能？ → 多重 SQUID 回路は実在する
  （量子コンピュータは数十〜数百の超伝導量子ビットを使う）。

  ── 候補 3: 周波数コム ──

  等間隔の周波数を同時にフィルタリングする回路。
  2f₁ の倍数全てにノッチを入れる。

  実現方法: 非線形ジョセフソン回路で
  f₂ の高調波を生成し、それでキャビティの
  偶数モードを駆動（飽和→実効的に吸収）。

  → これは「パラメトリック駆動」に近い。
  超伝導回路では標準的な技法。
""")

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ 既存のカシミールデータに素数ミュートの痕跡はあるか
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  カシミール効果の実験:
    Lamoreaux (1997): 2枚の平行板、精度 5%
    Mohideen & Roy (2002): 球-板配置、精度 1%
    各種の後続実験: 精度 ~0.1%

  しかしこれらは全て「全モードの制限」であり、
  「特定の素数のモードだけの制限」ではない。

  カシミール力: F = -π²ℏc/(240 a⁴) × A  （3D、平行板）

  この公式に「素数構造」は入っていない。
  240 = |K₇(Z)| = 2/|ζ(-3)| だが、これは ζ(-3)=1/120 と
  3D の因子 2 の組み合わせであり、素数ミュートとは無関係。

  ★ 既存のカシミールデータには素数ミュートの情報はない。
    理由: 標準のカシミール効果は「全モード」の効果であり、
    「特定の素数のモードだけ」を選択的に扱わない。
""")

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ 壁挿入と純粋ミュートの差を定量化
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
""")

# Pure ζ_{¬2} muting (remove even modes, add nothing):
# E_pure = (πℏc)/(2L) × ζ_{¬2}(-1) = (πℏc)/(2L) × (1/12)

# Wall insertion (remove even, add sub-cavity modes):
# E_wall = -(πℏc)/(6L)

# Difference:
# E_pure - E_wall = (πℏc)/(2L)(1/12) - (-(πℏc)/(6L))
#                 = (πℏc)/(24L) + πℏc/(6L)
#                 = (πℏc)/L × (1/24 + 4/24) = 5πℏc/(24L)

E_pure = 1/24  # coefficient: +1/12 × 1/2 = 1/24
E_wall_total = -1/6  # two L/2 cavities
diff = E_pure - E_wall_total

print(f"  純粋ζ₂ミュート: E = +(πℏc)/(24L) [奇数モードの和のみ]")
print(f"  壁挿入:          E = -(πℏc)/(6L)  [奇数 + 小キャビティ]")
print(f"  差: {diff:.4f} × πℏc/L = {diff*24:.0f}/24 × πℏc/L")
print()
print(f"  ★ 純粋ミュートと壁挿入は符号すら違う！")
print(f"    純粋: +（正のエネルギー = 反発）")
print(f"    壁: -（負のエネルギー = 引力）")
print()

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ 実験設計の根本的再考
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  問題:
  (a) 壁挿入 ≠ 素数ミュート（新しいモードが生まれる）
  (b) SQUID 1個 ≠ 素数ミュート（1モードしか消せない）
  (c) 既存カシミールデータに素数情報なし

  ── 正しい実験設計 ──

  目標: 偶数モードを「消すだけ」で「新しいモードを生まない」。

  方法: 吸収型フィルタ（壁ではなく減衰器）

  超伝導キャビティの中に「偶数モードだけに結合する減衰素子」
  を入れる。具体的には:

  (1) N 個のトランスモン量子ビット
      各々を f₂, f₄, f₆, ... にチューニング。
      トランスモンが「吸収→散逸」することでモードを減衰。
      新しい共鳴モードは生まない（散逸型）。

  (2) パーセル効果の利用
      偶数モードを外部の散逸的回路にパーセル結合させる。
      偶数モードのエネルギーが外部に漏洩 → 実効的にモード消滅。
      奇数モードは結合しない設計（対称性で分離可能）。

  ★ (2) が最も実現可能。

  設計:
    キャビティの L/2 地点に小さなカプラを設置。
    偶数モードは L/2 で振幅最大 → カプラと強結合。
    奇数モードは L/2 で振幅ゼロ → カプラと結合しない。
    カプラの先に抵抗（50Ω）を接続 → 偶数モードのエネルギーが散逸。

  これは「壁」ではなく「穴」。
  壁は反射（新モード生成）、穴は吸収（モード消滅）。
""")

# Q factor analysis
print("  偶数モードの Q 値の変化:")
print()

Q_intrinsic = 1e6  # cavity Q
for n in range(1, 9):
    if n % 2 == 0:
        # Even: coupled to dissipator
        # Purcell-limited Q: Q_purcell = Δ²/(κ_ext × g²)
        # For strong coupling at L/2: g ~ f_n × (coupling capacitance)
        # Crude estimate: Q_ext ~ 100 (strongly coupled)
        Q_total = 1/(1/Q_intrinsic + 1/100)
        status = "★ 吸収（Q～100）"
    else:
        # Odd: L/2 is a node, no coupling
        Q_total = Q_intrinsic
        status = "通常（Q～10⁶）"
    print(f"    モード {n}: f={n*6:.0f} GHz, Q={Q_total:.0e}, {status}")

print()

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ 修正された実験プロトコル
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Config A（全モード生存）:
    キャビティ + 量子ビット（Λ/2 カプラ OFF）
    → 全モード (n=1,2,3,4,...) が高 Q で共鳴
    → 真空エネルギー ∝ ζ(-1) = -1/12

  Config B（偶数モードミュート = 素数2ミュート）:
    キャビティ + 量子ビット（L/2 カプラ ON）
    → 偶数モード (n=2,4,6,...) が散逸、Q ～ 100 に低下
    → 奇数モード (n=1,3,5,...) は高 Q のまま
    → 真空エネルギー ∝ ζ_{¬2}(-1) = +1/12

  測定:
    Config A と Config B で量子ビットの周波数シフトを比較。
    分散シフトを E_J スイープで除去した残差の符号を見る。

  ζ予測: 残差の符号が A → B で反転する。
  標準QED: 残差の符号は変わらない（偶数モードの寄与が減るだけ）。

  ★★ これが「素数2ミュート」の正しい実験設計。
""")

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ 素数3ミュートの設計
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ζ_{¬3}(-1) = 1+2+4+5+7+8+10+... (3で割れない数の和)
             = ζ(-1) × (1-3^{-(-1)}) = (-1/12)(1-3) = +1/6

  3で割れるモード (n=3,6,9,12,...) を消す:
    L/3 の地点にカプラを設置。
    3の倍数モードは L/3 で振幅最大 → 結合して散逸。
    それ以外は L/3 での振幅はゼロでない → 部分的に結合...

  ★ L/3 に置いたカプラは 3 の倍数だけでなく、
    他のモードにも部分的に結合してしまう。

  正確には: モード n の L/3 での振幅 ∝ sin(nπ/3)
    n=1: sin(π/3) = √3/2 ≠ 0（結合する！）
    n=2: sin(2π/3) = √3/2 ≠ 0（結合する！）
    n=3: sin(3π/3) = 0（節 → 結合しない）

  逆だ！ L/3 に節があるのは n=3,6,9,... のモード。
  つまりカプラは n≠3の倍数に結合し、
  n=3の倍数には結合しない。

  → L/3 にカプラを置くと「3の倍数以外を消す」= ζ_{¬3} の逆。

  正しくやるには: 3の倍数だけに選択的に結合する方法が必要。

  → 簡単ではない。対称性を使った分離が必要。
  → 素数2（L/2）は対称性が最も単純なので最もやりやすい。
""")

# ============================================================================
print("""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ■ 結論
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  (1) モード2ミュート ≠ 素数2ミュート
      前者は1つのモード、後者は偶数モード全部。

  (2) 壁の挿入 ≠ 素数ミュート
      壁は新しいモードを生む（小キャビティ）。
      符号すら違う（壁: 負、純粋ミュート: 正）。

  (3) 正しい設計: L/2 に散逸型カプラ
      偶数モードを吸収（散逸）、奇数を保存。
      壁ではなく穴。反射ではなく吸収。

  (4) これは実験技術として実現可能
      パーセル効果によるモード選択的散逸は
      circuit QED の標準技術。

  (5) 既存のカシミールデータには素数情報なし
      標準カシミールは全モードの効果。
      素数ミュートの実験は未実施。

  (6) 素数2が最も実験しやすい
      L/2 の対称性が偶奇を完全に分離する。
      他の素数（3, 5, ...）は対称性が不完全で
      選択的ミュートが難しい。

  ── 実験のコストと変更点 ──

  元の設計からの主な変更:
    SQUID（1モード吸収）→ L/2 散逸カプラ（偶数全モード吸収）
    追加コスト: カプラ用の外部回路（～5万円）
    削減コスト: 精密 SQUID チューニングが不要になる

  全体コスト: ～60万円（ほぼ変わらず）
  実験の質: 大幅に向上（本物の素数ミュートをテスト）
""")

print("=" * 70)
print("  END")
print("=" * 70)
