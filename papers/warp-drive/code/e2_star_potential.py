#!/usr/bin/env python3
"""E2* potential: V(τ) = Ω_Λ + C|E2*(τ)|²/y². E2*(i) = 0 exactly."""
# The key discovery: E2*(τ=i) = 0 (exact), so the CM modulation
# vanishes today. BC gives w=-1, CM gives a SMALL correction.
# V = 2π/9 + C×|E2*|²/y². At τ=i: V = 2π/9 (pure cosm. constant).
# DESI's w≈-0.83 means τ is slightly off from i.
